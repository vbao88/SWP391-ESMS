{
  "name": "eyewear-shop-management-system",
  "version": "0.1.0",
  "private": true,
  "description": "ESMS monorepo starter for SWP391",
  "engines": {
    "node": "^20.19.0 || >=22.13.0"
  },
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "dev": "concurrently -n CLIENT,SERVER -c auto \"npm run dev --prefix client\" \"npm run dev --prefix server\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix client",
    "test": "npm run test --prefix client && npm run test --prefix server",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "10.0.3"
  }
}